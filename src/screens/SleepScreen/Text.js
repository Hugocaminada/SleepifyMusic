import styled from 'styled-components'
import { Color, Padding } from '../../styles'

export const HeaderText = styled.Text`
    font-size: 16px;
    color: ${Color.White};
    font-weight: bold;
`

export const BigHeaderText = styled.Text`
    font-size: 18px;
    color: ${Color.Black};
    font-weight: bold;
        paddingTop: ${Padding.L}px;
`

export const SubText = styled.Text`
    font-size: 10px;
    color: ${Color.White};
`

export const SettingsText = styled.Text`
    font-size: 12px;
    color: ${Color.White};
    font-weight: bold;
    paddingTop: ${Padding.S}px;

`
